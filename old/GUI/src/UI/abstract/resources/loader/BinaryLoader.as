package UI.abstract.resources.loader
{
	import UI.abstract.resources.item.LoadObj;
	
	public class BinaryLoader extends BaseLoader
	{
		public function BinaryLoader(loadObj:LoadObj)
		{
			super(loadObj);
		}
	}
}